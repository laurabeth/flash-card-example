import React from "react";

// Should display the 'zoomed in' view of a stack of flash cards
// One card at a time, title side first

const StackView = () => {
  return (
    <div>
      <p>This is the stack view</p>
    </div>
  );
};

export default StackView;
